---
description: Mobile-first (min-width) подход к вёрстке — базовые стили = мобилка (375px), адаптивы наращиваются вверх
alwaysApply: true
---

# Mobile-first

Проект использует **mobile-first** подход. Базовые стили без медиа-запросов — это мобильная версия (375px). Адаптивы добавляются через `min-width` строго снизу вверх.

## Брейкпоинты

```
$bp-tablet-sm:  768px;
$bp-tablet:     1024px;
$bp-desktop:    1440px;
$bp-desktop-xl: 1920px;
```

Mobile (375px) — дефолт, для него брейкпоинт не нужен.

## Миксин

```scss
@mixin from($bp) {
  @media (min-width: $bp) {
    @content;
  }
}
```

## Порядок стилей в SCSS-модулях

Базовые стили (мобилка) идут первыми, затем `@include from(...)` строго по возрастанию. Каждый медиа-блок содержит только **дельту изменений**, а не полный набор стилей.

```scss
// ПРАВИЛЬНО
.card {
  padding: 16px;
  font-size: 14px;

  @include from($bp-tablet-sm) {
    padding: 24px;
  }

  @include from($bp-desktop) {
    padding: 32px;
    font-size: 16px;
  }
}

// НЕПРАВИЛЬНО — max-width, desktop-first
.card {
  padding: 32px;
  @media (max-width: 767px) {
    padding: 16px;
  }
}
```

## Компоненты (Next.js)

- Серверные компоненты по умолчанию. `"use client"` только при реальной интерактивности (формы, обработчики событий, хуки).
- Адаптивность — через CSS, а не через JS-проверку ширины экрана.
- Не использовать `display: none` для скрытия крупных блоков контента на мобилке — вместо этого пересматривать структуру.

## Порядок сборки

1. Сначала собрать мобильную версию секции целиком.
2. Затем добавить адаптивы: 768 → 1024 → 1440 → 1920.
3. Не откладывать адаптивы «на потом» — верстать в рамках одной секции сразу.
